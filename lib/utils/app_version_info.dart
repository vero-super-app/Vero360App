/// App marketing version + build number.
/// Keep in sync with `pubspec.yaml` → `version: x.y.z+build`.
class AppVersionInfo {
  AppVersionInfo._();

  static const String version = '1.1.5';
  static const String buildNumber = '10009';
}
